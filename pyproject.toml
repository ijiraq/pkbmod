[project]
name = "pkbmod"
version = "0.1.0"
description = "Predictive Clustering Kernel-Based Moving Object Detection"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.10.0",
]

[dependencies-groups]
dev = [
    "pytest>=8.3.3",
]
